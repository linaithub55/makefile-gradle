plugins {
    id 'java'
    id 'application'
}

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'junit:junit:4.13.2'
}

application {
    mainClass = 'chess.Main'
}

test {
    useJUnit()
    testLogging {
        events "passed", "skipped", "failed"
    }
}
